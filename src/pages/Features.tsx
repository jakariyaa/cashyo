import { FeaturesHero } from "@/components/common/features-hero";
import { FeaturesList } from "@/components/common/features-list";

const Features = () => {
  return (
    <main>
      <FeaturesHero />
      <FeaturesList />
    </main>
  );
};

export default Features;
